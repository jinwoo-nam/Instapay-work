import 'package:flutter/material.dart';

class ServiceTerminationScreen extends StatelessWidget {
  const ServiceTerminationScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(

      ),
      body: Text(''),
    );
  }
}
