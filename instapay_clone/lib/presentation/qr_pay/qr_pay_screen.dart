import 'package:flutter/material.dart';

class QrPayScreen extends StatelessWidget {
  const QrPayScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('qr pay screen');
  }
}
